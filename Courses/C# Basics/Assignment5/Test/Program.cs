namespace Test;

class Program
{
    static void Main(string[] args)
    {
        double radius = 5;
        double area = Math.PI * Math.Pow(radius, 2);
            Console.WriteLine(area);
    }
}